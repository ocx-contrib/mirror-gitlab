# NOTICE

This repository packages and redistributes upstream
[GitLab CLI (`glab`)](https://gitlab.com/gitlab-org/cli).

The Apache-2.0 license covers the OCX pipeline files authored here. It does
**not** cover upstream-derived assets — the `glab` binaries published to
`ocx.sh/glab` (MIT, GitLab B.V.) and the GitLab logo (GitLab B.V. trademark,
used for catalog identification under nominative-fair-use).
